const EMBED_SIZE = 'embed-size';
const INITIAL_MESSAGE = 'frames-init';
const AMP_SENTINEL = 'amp';
const FRAME_PREFIX = 'data-frame-';
const FRAME_AUTO_INITIALIZED = `${FRAME_PREFIX}auto-initialized`;
const FRAME_SRC = `${FRAME_PREFIX}src`;
const FRAME_ATTRIBUTE_PREFIX = `${FRAME_PREFIX}attribute-`;

export {
	AMP_SENTINEL,
	EMBED_SIZE,
	FRAME_ATTRIBUTE_PREFIX,
	FRAME_AUTO_INITIALIZED,
	FRAME_PREFIX,
	FRAME_SRC,
	INITIAL_MESSAGE,
};
